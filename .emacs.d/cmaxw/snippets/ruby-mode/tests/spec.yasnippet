# -*- mode: snippet -*-
#name : RSpec spec
#key : spec
# --
require "spec_helper"

describe `(let ((fn (capitalize (file-name-nondirectory
                                 (file-name-sans-extension
                 (or (buffer-file-name)
                     (buffer-name (current-buffer))))))))
                     (replace-regexp-in-string "_\\|Spec" "" fn))` do
  $0
end