# -*- mode: snippet -*-
# name: attr_accessor
# key: wr
# key: rw
# --
# ${1:fields}$0
${1:$
(mapconcat
        '(lambda (x) (concat "attr_accessor :" x))
        (split-string yas-text " ")
        (concat "\n" (make-string (current-column) 32)))}
