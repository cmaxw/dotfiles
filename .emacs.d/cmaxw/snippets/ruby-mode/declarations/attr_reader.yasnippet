# -*- mode: snippet -*-
# name: attr_reader
# key: r
# --
# ${1:fields}$0
${1:$
(mapconcat
        '(lambda (x) (concat "attr_reader :" x))
        (split-string yas-text " ")
        (concat "\n" (make-string (current-column) 32)))}
